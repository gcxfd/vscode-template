{
    "editor.formatOnSave": true,
    "saveAndRun": {
        "commands": [
            {
                "match": ".*",
                "cmd": "clear && echo -e `date +\"%Y-%m-%d %H:%M:%S\"`'\n'&&cd ${workspaceRoot} && bat ${file} && echo -e '\n'"
            }
        ]
    }
}